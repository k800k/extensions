# Contributing

Contributions certify the Developer Certificate of Origin with `git commit -s`.

Before changing an extension from `approvalRequired`, complete its specification, privacy and rights records, host/authentication declarations, contract suite, live smoke-test record, and review status. Package hash changes invalidate prior MangaReader approval.

Run `npm run check && npm test && npm run bundle && npm run publish:dry-run` before opening a pull request.
