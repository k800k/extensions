# AniList privacy assessment

Search text, AniList media identifiers, progress, scores, and OAuth bearer tokens are sent only to AniList endpoints. Tokens remain in MangaReader's device-only extension Keychain namespace and are not synchronized.
