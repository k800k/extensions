# AniList audit status

The implementation and manifest are open for user review and are not certified safe by MangaReader. Contract checks cover its declared operations, hosts, permissions, package shape, and API compatibility.
