# AniList tracker specification

This package uses AniList OAuth implicit authorization and the official GraphQL API. It supports title search, reading-progress lookup and mutation, and list collections. Access tokens are supplied by MangaReader through isolated secure state.
