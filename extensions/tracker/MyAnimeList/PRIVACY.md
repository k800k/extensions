# MyAnimeList privacy assessment

Search text, manga identifiers, progress, scores, and OAuth tokens are sent only to MyAnimeList endpoints. Tokens remain in MangaReader's device-only extension Keychain namespace and are not synchronized.
