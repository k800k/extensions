# MyAnimeList tracker specification

This package uses MyAnimeList OAuth 2 with a plain PKCE challenge, as required by the service, and API v2. It supports manga search, progress lookup and updates, and paginated user-list collections.
